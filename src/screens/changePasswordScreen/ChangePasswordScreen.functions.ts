import { changePasswordProps } from './ChangePasswordScreen.types';

export const changePassword = ({navigation}: changePasswordProps) => {
    //INGREGRATE WITH API
    navigation.navigate("Login");
} 