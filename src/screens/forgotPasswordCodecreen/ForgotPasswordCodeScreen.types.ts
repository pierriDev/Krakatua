import type { Navigation, Route } from '@/common/types';

export interface ForgotPasswordCodeProps {
  navigation: Navigation;
  route: Route;
}

export interface ForgotPasswordCodeViewProps {
  navigation: Navigation;
}

export interface sendCodeProps {
  navigation: Navigation;
}
