import type { Navigation, Route } from '@/common/types';

export interface ForgotPasswordEmailProps {
  navigation: Navigation;
  route: Route;
}

export interface ForgotPasswordEmailViewProps {
  navigation: Navigation;
}

export interface sendEmailProps {
  navigation: Navigation;
}
